{
  "name": "periodismo-independiente-qr",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.12.3",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.3.0",
    "gh-pages": "^6.3.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
  "dev": "astro dev",
  "build": "astro build",
  "postbuild": "echo > dist/.nojekyll",
  "preview": "astro preview",
  "deploy": "npm run build && gh-pages -d dist"
  }
}
